name: 打包React项目

on: push

jobs:
  npm-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Install dependencies
      run: npm install
    - name: Package project
      run: npm run build